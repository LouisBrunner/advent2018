use core;

pub type Error = core::Error;

// pub fn get_data(session: &str) -> Result<SOMETHING, core::Error> {
//     let content = try!(core::request(session, XX));
//
//     parse_values(content.trim().split("\n"))
// }

// pub fn parse_values<'a, Iin>(logs: Iin) -> Result<SOMETHING, core::Error>
// where
//     Iin: Iterator<Item = &'a str>,
// {
//     TODO
// }
